const DefaultConfig = {
  el: '',
  width: 1100,
  height: 450,
  toolbar: {                // 菜单栏配置
    bold: true,             // 加粗
    strikethrough: true,    // 删除线
    underline: true,        // 下划线
  }
}

export default DefaultConfig;